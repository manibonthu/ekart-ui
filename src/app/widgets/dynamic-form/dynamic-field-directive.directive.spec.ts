import { DynamicFieldDirectiveDirective } from './dynamic-field.directive';

describe('DynamicFieldDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFieldDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
